# LiLiAi Android Album

GitHub Actions builds the Android APK automatically. The album HTML is kept visually/behaviorally based on the tested working version, while the 12 MP3 data blobs are stored as separate local assets so GitHub web upload can handle them.
